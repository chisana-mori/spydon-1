# 基础设施目录说明

- `docker/`：Docker Compose 及相关容器编排文件  
- `k8s/`：Kubernetes 清单与部署样例  
- `scripts/`：部署、调试与运维脚本  
- `cas/`：CAS 服务相关配置与运行时数据，其中  
  - `config/`：版本化配置与服务注册文件  
  - `runtime/`：本地运行产生的挂载目录、生成的 keystore 等

运行或修改基础设施脚本前，请先阅读对应子目录内的说明，并确认敏感信息不会被提交到版本库。
